{
  "name": "rize-os-job-portal",
  "version": "1.0.0",
  "description": "Job & Networking Portal with Web3 and AI features",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["job-portal", "web3", "ai", "mern-stack"],
  "author": "RizeOS Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 